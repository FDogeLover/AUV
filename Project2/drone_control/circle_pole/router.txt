# 阶段1：2m直线巡航，单杆验证用。杆塔实测摆放建议：路径旁y偏移0.2~0.4m处，
# 避免直接摆在航线正中间（悬停避让会在0.75m处触发，验证的是能不能先悬停
# 再切换到环绕，而不是一路顺畅冲上去）。
# 2026-07-13：环绕半径已从0.5m上调到0.7m(中心到中心，机身等效半径约0.4m，
# 见Mission_GPT.py里POLE_CIRCLE_RADIUS_M的注释)。推荐杆塔坐标(1.0, 0.3)，
# 环绕飞行实际覆盖范围约x∈[0.3,1.7]、y∈[-0.4,1.0]，测试空间要留够这块区域，
# 且这块区域内不能有其他障碍物/墙角(墙角误判见设计文档已知限制)。
0.0,0.0,1.2
0.5,0.0,1.2
1.0,0.0,1.2
1.5,0.0,1.2
2.0,0.0,1.2
