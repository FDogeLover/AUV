# 立体货架盘点地面站通信

- 板端通信设备为 `/dev/bt_serial`；
- 链路支持广播；
- 当前处于训练阶段，没有地面站接收端，因此发送端不能等待 ACK、不能因无人接收
  阻塞或中止飞行；
- 训练时仍可广播任务状态和识别结果，同时必须在机载本地日志/结果存储中留档；
- 将来接入正式地面站时再启用双向可靠模式，广播训练模式和可靠模式必须配置化，
  不能把 ACK 依赖写死进任务状态机。
