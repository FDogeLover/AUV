# 立体货架盘点赛题坐标系

`drone_control/warehouse_inventory` 的飞行路径坐标约定：

- T265 局部原点设在起飞点；
- `+Y` 对应场地 5 m 长边方向；
- `-X` 对应场地 4 m 宽边方向；
- `+Z` 竖直向上。

图纸坐标与飞行坐标必须通过统一变换函数转换，不能在路线和货位代码中各自交换
X/Y或散落正负号。若图纸使用 `u` 表示5 m长边、`v` 表示4 m宽边，起飞点图纸
坐标为 `(u0, v0)`，则基础映射为：

`x_flight = -(v - v0)`，`y_flight = u - u0`，`z_flight = z`。

最终仍需用实物摆放方向和T265安装方向做手持/拆桨验证，不能只凭图纸确认符号。

## 已确认绕行约束

- 2026-07-20 route-only 40航点完整实飞走完全程；现场观察原下端通道
  `X=+0.15 m` 有擦到板子的风险；
- 用户明确确认将下端通道进一步向外移动到 `X=+0.30 m`；注意这里是飞行坐标
  的正X，不能误写成 `-0.30 m`，后者会进入模型中的货架范围；
- `WarehouseConfig.lower_bypass_x_m`、规划器和
  `router_full_inventory_test.txt` 四个下绕点已统一改为 `+0.30 m`，板端同步后
  直接生成路线验证通过；
- 其他24个扫码货位坐标不因route-only测试一次性调整，后续须根据各货位真实扫码
  画面逐点微调。
