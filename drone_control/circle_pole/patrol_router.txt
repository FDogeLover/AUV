# 阶段1：3.5m直线巡航，单杆验证用。杆塔实测摆放建议：路径旁y偏移0.2~0.4m处，
# 避免直接摆在航线正中间（悬停避让会在0.75m处触发，验证的是能不能先悬停
# 再切换到环绕，而不是一路顺畅冲上去）。
# 2026-07-13：环绕半径已从0.5m上调到0.7m(中心到中心，机身等效半径约0.4m，
# 见Mission_GPT.py里POLE_CIRCLE_RADIUS_M的注释)。推荐杆塔坐标(0.3, 1.0)，
# 环绕飞行实际覆盖范围约x∈[-0.4,1.0]、y∈[0.3,1.7]，测试空间要留够这块区域，
# 且这块区域内不能有其他障碍物/墙角(墙角误判见设计文档已知限制)。
# 2026-07-15：巡航高度从1.2m改成1.5m(赛题要求150±10cm)，此前是阶段1测试
# 空间限制值。半径本次不改，净空约30cm仍低于赛题40~60cm要求，留到以后
# 专项调整。
0.0,0.0,1.5
0.0,0.5,1.5
0.0,1.0,1.5
0.0,1.5,1.5
0.0,2.0,1.5
0.0,2.5,1.5
0.0,3.0,1.5
0.0,3.5,1.5
